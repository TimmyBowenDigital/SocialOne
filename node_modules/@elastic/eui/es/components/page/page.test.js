function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

import React from 'react';
import { render } from 'enzyme';
import { requiredProps } from '../../test/required_props';
import { EuiPage } from './page';
describe('EuiPage', function () {
  test('is rendered', function () {
    var component = render(React.createElement(EuiPage, requiredProps));
    expect(component).toMatchSnapshot();
  });
  describe('restrict width', function () {
    test('can be set to a default', function () {
      var component = render(React.createElement(EuiPage, _extends({}, requiredProps, {
        restrictWidth: true
      })));
      expect(component).toMatchSnapshot();
    });
    test('can be set to a custom number', function () {
      var component = render(React.createElement(EuiPage, _extends({}, requiredProps, {
        restrictWidth: 1024
      })));
      expect(component).toMatchSnapshot();
    });
    test('can be set to a custom value and measurement', function () {
      var component = render(React.createElement(EuiPage, _extends({}, requiredProps, {
        restrictWidth: "24rem"
      })));
      expect(component).toMatchSnapshot();
    });
  });
});
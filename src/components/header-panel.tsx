import React, { Component } from "react";
import Button from "./ui-elements/button"
import NavPanel from "./nav-panel";

class HeaderPanel extends Component {
    render() {
        return (
            <p>This is the Header Panel</p>
        );
    }
}

export default HeaderPanel